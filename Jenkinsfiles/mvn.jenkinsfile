@Library('jenkinslib') _

def demo = new org.devops.demo()

pipeline{
	agent{
		node {
			label "master"
		}
	}

	stages{
		stage('mvn'){
			steps{
				script{
					mvn_home = tool "mvn"	//全局工具
					demo.PrintMes("$mvn_home",'green')
					sh "$mvn_home/bin/mvn -h"
				}
			}
		}
	}
}
